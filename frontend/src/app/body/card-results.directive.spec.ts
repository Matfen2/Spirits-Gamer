import { CardResultsDirective } from './card-results.directive';

describe('CardResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new CardResultsDirective();
    expect(directive).toBeTruthy();
  });
});
